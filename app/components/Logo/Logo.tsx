import { FC } from 'react';
import MainButton from '@/ui/buttons/Logo/MainButton';
import MainLogo from '@/ui/images/Logo/MainLogo';

const Logo: FC = () => {
  return (
    <MainButton>
      <MainLogo />
    </MainButton>
  )
}

export default Logo
